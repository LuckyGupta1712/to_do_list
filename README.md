# 📝 My To-Do List

A simple, modern, and responsive **To-Do List web application** built using **vanilla HTML, CSS, and JavaScript**.  
It allows users to add tasks, mark them as completed, and delete them, with data persisted using **localStorage**.

---

## ✨ Features

- Add new tasks
- Mark tasks as completed
- Delete tasks
- Persistent storage using `localStorage`
- Clean and minimal UI
- Smooth animations and hover effects
- Fully responsive (mobile & desktop)
- Keyboard support (Press **Enter** to add tasks)
- Empty state message when no tasks are present

---

## 🛠️ Tech Stack

- **HTML5** – structure & accessibility  
- **CSS3** – styling, animations, responsiveness  
- **JavaScript (Vanilla)** – logic & state management  

---

## 📂 Project Structure

